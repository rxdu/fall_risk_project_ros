PKG_CONFIG_PATH=/usr/local/lib/pkgconfig ./configure --prefix=/usr --disable-eds

sudo apt-get install intltool libgconf2-dev libsasl2-dev libxv-dev libdbus-glib-1-dev


uninstall libopal and install the opalfrom git